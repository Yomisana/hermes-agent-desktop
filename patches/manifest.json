{
  "schemaVersion": 1,
  "patches": [
    {
      "id": "desktop-remote-only",
      "version": 2,
      "scope": "desktop-client",
      "status": "local",
      "apply": "scripts/apply-patch.py",
      "description": "Disable automatic local runtime bootstrap; users configure the remote gateway explicitly."
    }
  ]
}
